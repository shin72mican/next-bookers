{
  "name": "bookers",
  "private": true,
  "engines": {
    "node": ">=22.3.0"
  },
  "workspaces": [
    "packages/front-app",
    "packages/api-app"
  ],
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "format": "prettier --write \"**/*.{ts,tsx,md}\""
  },
  "dependencies": {
    "turbo": "^2.3.4"
  },
  "packageManager": "npm@10.8.1"
}
