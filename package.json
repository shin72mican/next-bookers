{
  "name": "bookers",
  "private": true,
  "engines": {
    "node": ">=22.3.0"
  },
  "workspaces": [
    "packages/front-app",
    "packages/api-app"
  ],
  "scripts": {
    "build": "turbo run build"
  },
  "dependencies": {
    "turbo": "^2.3.4"
  }
}
